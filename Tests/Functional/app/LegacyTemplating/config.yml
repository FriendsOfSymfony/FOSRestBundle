imports:
    - { resource: ../config/default.yml }

framework:
    templating:
        engines: ['twig']

fos_rest:
    view:
        view_response_listener: 'force'

twig:
    strict_variables: '%kernel.debug%'
