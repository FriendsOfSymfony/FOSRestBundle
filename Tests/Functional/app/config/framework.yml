framework:
    secret: test
    router: { resource: "%kernel.project_dir%/config/routing.yml" }
    test: ~
    csrf_protection: ~
    form: ~
    session:
        storage_id: session.storage.mock_file
    default_locale: en
